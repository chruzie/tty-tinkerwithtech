# Daily Audit — 2026-03-18

**Branch audited:** `ralph/prd-implementation` (most recent commit: `16af196` — 2026-03-17)
**Agents run:** Ralph/PRD · Security · Code Review · UI/UX
**Tests at audit time:** 124 passing, 0 failing
**Previous audit:** [docs/audits/2026-03-16-audit.md](./2026-03-16-audit.md)
**Net new commits since last audit:** 0 — all findings from 2026-03-16 remain in the exact state audited

---

## Ralph / PRD Gaps

### Items resolved since 2026-03-16 audit

| ID | Fix |
|----|-----|
| C-P1-3 | Embedding preload in `lifespan` now degrades gracefully with warning log (no longer blocks startup) |
| C-P1-4 | `registry.py` module docstring now correctly reflects provider order |
| U-P0-2 | Rate-limit banner now `hidden` by default in `web/index.html` (still broken in `mockup.html`) |
| U-P2-1 | Generate button in `web/index.html` now has spinner + disabled state during requests |
| U-P2-2 | `startDailyLimitCountdown()` and `startCooldown()` implemented in `web/index.html` |
| M-1 | SSRF redirect bypass resolved — `httpx 0.27+` defaults to `follow_redirects=False` |

### Still open from 2026-03-16

All items not listed above remain unresolved. Highest-priority unresolved:

| ID | Severity | Description |
|----|----------|-------------|
| H-1 | HIGH | `POST /v1/themes` unauthenticated — gallery flooding |
| H-2 / N-H-1 | HIGH | ANSI injection via raw `theme.name` in `/v1/neofetch/{slug}` |
| H-4 | HIGH | Non-constant-time Bearer token comparison on `/metrics` |
| BUG-1 | HIGH | `log_cost()` never called → `DAILY_SPEND_CAP` always $0 → spend cap non-functional |
| C-P0-1 / M-5 | HIGH | `increment_download_count` TOCTOU race (SQLite AND Firestore) |
| C-P0-2 / M-2 | HIGH | `assert` in `safe_fetch` SSRF path stripped by Python `-O` |
| C-P0-3 | HIGH | `TTLCache` check-then-set not thread-safe in `api/middleware.py` |
| U-P0-1 | HIGH | Design system split — `web/` and `mockup.html` are visually unrelated |

### New PRD gaps (not in 2026-03-16 report)

| ID | Location | Description |
|----|----------|-------------|
| GAP-NEW-1 | PRD §12, line ~341 | §12 still says "valid JSON matching the 21-key schema" — contradicts §8 (corrected to key=value) and implementation. §8 was fixed but §12 was missed. |
| GAP-NEW-2a | DEPLOY.md:42–43,71–72 | Provisions `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` secrets and injects them into Cloud Run. No code uses them. PRD v1.3 dropped GitHub OAuth entirely. Stale from pre-v1.3. |
| GAP-NEW-2b | PRD Deployment vs DEPLOY.md:66,73 | Memory discrepancy: PRD says `--memory=512Mi`, DEPLOY.md says `256Mi`. DEPLOY.md has `--no-allow-unauthenticated` which would block all unauthenticated API calls from the web frontend. |

### Open questions (escalated)

| ID | Status | Description |
|----|--------|-------------|
| **OQ-1** | 🔴 **ESCALATED TO SHIPPING BUG** | iTerm2 download button in `web/index.html` produces **Ghostty-format output**, not XML plist. The client-side `ghosttyToIterm2` conversion referenced in PRD §10 does not exist in `web/index.html`. `ITerm2Serializer` exists server-side but is never called from any endpoint. Both buttons download Ghostty format. |
| OQ-2 | Open | Slug conflicts for generated (unpublished) themes — no resolution in PRD |
| OQ-3 | Open | `GET /v1/themes` list response — full `theme_data` vs preview colors |

### PRD corrections needed

| Section | Issue |
|---------|-------|
| §7 burst threshold | "If the same IP makes > 3 requests within 60 seconds" — code uses `>= 3` (`_BURST_THRESHOLD=3`, check `len(recent) >= _BURST_THRESHOLD`). Off-by-one: users are penalized on the 3rd request, not the 4th. |
| §8 SYSTEM_PROMPT block | PRD block still shows old JSON format. Implementation (`generator/prompt.py`) uses key=value. PRD must be corrected. |
| §12 Security Rules | "valid JSON matching the 21-key schema" → "valid Ghostty key=value format with all 21 required hex keys" |
| §16 Phase 4 status | Marked `✓ done` but includes "spend cap" which is non-functional (BUG-1). Should be annotated. |
| Deployment Cloud Run command | Memory: `512Mi` (PRD) vs `256Mi` (DEPLOY.md). `--no-allow-unauthenticated` flag in DEPLOY.md absent from PRD and would break the web frontend. |

---

## Security Findings (2026-03-18 delta)

**Dependency CVEs:** `pyasn1 0.6.2` — **CVE-2026-30922 HIGH** (fix: `pyasn1>=0.6.3`, run `uv lock --upgrade-package pyasn1`)

### Prior findings status

| ID | Status |
|----|--------|
| H-1 Unauthenticated publish | ❌ STILL OPEN |
| H-2 ANSI injection (neofetch name) | ❌ STILL OPEN — palette injection fixed; name-field vector open |
| H-3 No per-slug download rate limit | ❌ STILL OPEN |
| H-4 Non-constant-time metrics token | ❌ STILL OPEN |
| M-1 SSRF redirect bypass | ✅ FIXED (httpx 0.27+ default) |
| M-2 `assert` in ssrf_guard stripped by `-O` | ❌ STILL OPEN |
| M-5 Download count TOCTOU | ❌ STILL OPEN (both SQLite and Firestore) |
| M-6 No length caps on PublishRequest | ❌ STILL OPEN |

### New security findings

| ID | Severity | File:Line | Description | Fix |
|----|----------|-----------|-------------|-----|
| N-H-2 | HIGH | `api/main.py:313,327` | Negative `offset` accepted in `GET /v1/themes`. Python slice wraps: `offset=-9999` with `limit=1` returns results bypassing pagination intent. | Add `offset = max(0, offset)` guard before slicing. |
| N-H-3 | HIGH | `api/middleware.py:95–102` | Token bucket `available()` check and `consume()` call are non-atomic in async context. Two concurrent coroutines both pass `available()` before either calls `consume()`, bypassing rate limit. | Use `asyncio.Lock` or combine check+consume atomically in `_TokenBucket`. |
| N-M-1 | MEDIUM | `security/ssrf_guard.py:131` | `safe_fetch(**kwargs)` forwards arbitrary caller-controlled kwargs to `httpx.get`. A future caller could pass `follow_redirects=True`, silently defeating SSRF protection. | Accept explicit allowlisted params only; remove `**kwargs`. |
| N-M-2 | MEDIUM | `api/main.py:321,350,385,423` | Four endpoints load up to 1000 full theme rows (including `theme_data` blobs) per request. DoS amplification: unauthenticated callers can inflate the table (H-1) and cause all reads to slow linearly. | Push sort/limit/offset to DB layer; add indexed `slug` column. |
| N-M-3 | MEDIUM | `api/middleware.py:117–126` | `AuditLogMiddleware` records `query_hash=""`, `provider="unknown"`, `tier_used=0` on every generate call. Audit trail is forensically useless. | Wire actual request metadata into the audit log from the route handler. |
| N-L-1 | LOW | `providers/openai_compat.py:54` | `httpx.Client(timeout=60.0)` uses a unified 60s timeout. A slow-loris upstream holds a thread for 60s. | Use `httpx.Timeout(connect=5.0, read=55.0, write=10.0, pool=5.0)`. |
| N-L-2 | LOW | `api/main.py:311` | `sort` query param accepts any string; non-`"downloads"` silently falls through to default. No allowlist. | Use `Literal["newest", "downloads"]` type or explicit validation. |
| N-L-3 | LOW | `pyproject.toml:57` | `S101` (assert) suppressed globally in ruff — the `assert` in `ssrf_guard.py:86` (M-2) will never be caught by linting. | Scope S101 ignore to `tests/*` only. |
| DEP-1 | HIGH | `pyasn1==0.6.2` | CVE-2026-30922 — fix in 0.6.3 | `uv lock --upgrade-package pyasn1` |

---

## Code Review Findings

**Test suite: 124 passed, 0 failed.**

### Prior findings status

| ID | Status |
|----|--------|
| C-P0-1 Download count TOCTOU (SQLite) | ❌ STILL OPEN |
| C-P0-2 `assert` in ssrf_guard | ❌ STILL OPEN |
| C-P0-3 TTLCache check-then-set not thread-safe | ❌ STILL OPEN |
| C-P1-1 No `slug` column / O(N) scans | ❌ STILL OPEN — 4th endpoint (neofetch) now also affected |
| C-P1-2 No ThemeRepositoryProtocol/ABC | ❌ STILL OPEN |
| C-P1-3 Useless embedding preload | ✅ FIXED |
| C-P1-4 Wrong docstring in registry.py | ✅ FIXED |

### New issues

| ID | Severity | File:Line | Description |
|----|----------|-----------|-------------|
| NC-P0-1 | P0 | `cache/firestore_db.py:196–205` | Firestore `increment_download_count` has same TOCTOU race as C-P0-1. Should use `firestore.Increment(1)` (already demonstrated in `log_cost` at line 134 of same file). |
| NC-P1-1 | P1 | `api/main.py:368` | `publish_theme` unauthenticated — same as H-1 but now confirmed code-path: no auth check anywhere in the handler |
| NC-P1-2 | P1 | `api/main.py:321–327` | `GET /v1/themes` loads 1000 rows on every browse request; `offset` param has no `max(0, offset)` guard |
| NC-P2-1 | P2 | `api/middleware.py:49–58` | `_get_minute_bucket` and `_get_hour_bucket` do double dict lookup (set then get separately) — store constructed object in local and return directly |
| NC-P2-2 | P2 | `api/main.py:443–445` | neofetch hex slice `hex_color[0:2]` raises `ValueError` on malformed stored theme (3-char hex, empty string). Community-published themes validated at write, but pre-seeded/manually inserted themes may not be. Add `try/except ValueError` with fallback. |
| NC-P2-3 | P2 | `api/main.py:311` | `sort` query param not validated against allowlist (same as N-L-2) |

### Still-open P2 technical debt

| ID | Description |
|----|-------------|
| C-P2-1 | `api/main.py:219,283` — bare `except Exception: pass` swallows embedding errors silently |
| C-P2-2 | Dead code: `build_prompt()`, `_IMAGE_REFINE_SYSTEM`, `security/rate_limiter.py` |
| C-P2-3 | CATALOGUE has 5 providers unreachable from Cloud Run — 3–5 failing `health_check()` calls per `generate_with_fallback()` |
| C-P2-4 | `FirestoreThemeRepository` has zero tests |
| BUG-1 | `log_cost()` never called after LLM generation → spend cap always $0 |
| BUG-2 | Burst offense escalation triggered on next allowed request, not on rejection |
| BUG-3 | `firebase.json` `public: "web/dist"` — directory doesn't exist; Firebase Hosting deploy fails |

---

## UI/UX Findings

### Prior P0 findings status

| ID | Status |
|----|--------|
| U-P0-1 Design system split | ❌ STILL OPEN — gap has deepened |
| U-P0-2 Rate-limit banner always visible | ✅ FIXED in `web/index.html`; ❌ still broken in `mockup.html` |
| U-P0-3 No `history.pushState()` | ❌ STILL OPEN (mockup.html SPA views; web/ multi-page links partially acceptable) |

### Prior P1 findings status

| ID | Status |
|----|--------|
| U-P1-1 `text-muted` contrast fails WCAG AA on surface | ❌ STILL OPEN |
| U-P1-2 `#byok-key-input` missing `<label>`/`aria-label` | ❌ STILL OPEN (mockup.html only; not in web/) |
| U-P1-3 Format toggle missing `aria-pressed` + non-color indicator | ❌ STILL OPEN |
| U-P1-4 Toast live region timing (content + unhide simultaneous) | ❌ STILL OPEN |
| U-P1-5 `role="radiogroup"` groups missing arrow-key nav | ❌ STILL OPEN |

### New issues found

| ID | Severity | Description |
|----|----------|-------------|
| N-U-P1-1 | P1 | `mockup.html:254` — `#byok-provider-select` has no `<label>`. The `<label for="provider-select">` targets wrong ID. |
| N-U-P1-2 | P1 | `mockup.html` gallery filter buttons (lines 826–828): no JS, no ARIA state (`aria-pressed`), clicking does nothing. Visually imply interactivity they don't have. |
| N-U-P1-3 | P1 | `web/gallery.html` sort buttons: active state is color-only (indigo vs gray), no `aria-pressed`. Same WCAG 1.4.1 failure as U-P1-3. |
| N-U-P1-4 | P1 | `web/theme.html` download buttons: interactive before theme loads; disabled state absent; AT users receive no feedback on activation before data arrives. |
| N-U-P2-1 | P2 | `web/index.html:72` — `#publish-name` input has no `<label>` or `aria-label`; placeholder is not a label substitute |
| N-U-P2-2 | P2 | `web/gallery.html` — theme cards created via `document.createElement('a')` have nested interactive copy button (`e.stopPropagation()`). Nested interactive elements inside `<a>` are invalid HTML; keyboard activation of copy button breaks card navigation for AT users. |
| N-U-P2-3 | P2 | `web/index.html` — `#error-banner` and `#result` div have no `role="alert"` or `aria-live`. Generate result and error states are invisible to AT. |
| N-U-P2-4 | P2 | `web/index.html` — `#publish-result` text revealed by `classList.remove('hidden')` with no live region (same timing issue as U-P1-4 for toast). |
| N-U-P3-1 | P3 | `mockup.html` / `web/*.html` — no `<meta name="description">` or Open Graph tags. Share URLs render with no preview. |
| N-U-P3-2 | P3 | `mockup.html` — view transition functions don't move keyboard focus to new view heading. Focus remains on trigger (now hidden). |

### Progress assessment

**Static / marginally improving.** Two P0 items partially resolved (U-P0-2 fixed in `web/`; U-P2-1, U-P2-2 fixed in `web/`). All P1 accessibility items unchanged. New UI surface (gallery, theme detail page) introduced new accessibility debt without addressing existing patterns. The design system split (U-P0-1) remains the single most blocking issue.

---

## Recommended Actions (Prioritized)

### 🔴 Immediate — fix before any production traffic

| # | ID | Action |
|---|----|--------|
| 1 | DEP-1 | **Upgrade pyasn1**: `uv lock --upgrade-package pyasn1` + commit updated lockfile |
| 2 | OQ-1 (escalated) | **Fix iTerm2 download**: implement client-side `ghosttyToIterm2()` conversion in `web/index.html`, or add `?format=iterm2` server endpoint. Both download buttons currently produce Ghostty format. |
| 3 | H-2 / N-H-1 | **Strip ANSI from neofetch name**: apply `sanitize_prompt` control-char filter to `name` before writing to `lines[]` |
| 4 | H-4 | **Constant-time metrics auth**: `hmac.compare_digest(auth, f"Bearer {token}")` |
| 5 | C-P0-1 / M-5 | **Atomic download counter (SQLite)**: `UPDATE themes SET download_count = download_count + 1 WHERE id = ?` |
| 6 | NC-P0-1 | **Atomic download counter (Firestore)**: `firestore.Increment(1)` (already used in `log_cost`) |
| 7 | C-P0-2 / M-2 | **Replace `assert` in `safe_fetch`**: `if hostname is None: raise ValueError(...)` |
| 8 | N-H-2 | **Clamp offset**: `offset = max(0, offset)` in `GET /v1/themes` |
| 9 | N-L-3 | **Scope S101 suppression to tests only** in `pyproject.toml` so the remaining `assert` is caught by linting |

### 🟠 Before production traffic

| # | ID | Action |
|---|----|--------|
| 10 | BUG-1 | Wire `log_cost()` call after successful LLM generation in `POST /v1/generate` |
| 11 | C-P0-3 / N-H-3 | Thread-safe TTLCache + async-safe token bucket (wrap with lock or atomic consume) |
| 12 | C-P1-1 / N-M-2 | Add indexed `slug` column to SQLite schema; push sort/offset to DB layer |
| 13 | U-P0-1 | Reconcile `web/` design system with `mockup.html` tokens (Space Mono, dark palette) |
| 14 | M-6 | `Annotated[str, Field(max_length=100)]` on `PublishRequest.name`; `max_length=4096` on `theme_data` |
| 15 | GAP-NEW-2a | Remove `GITHUB_CLIENT_ID`/`SECRET` from DEPLOY.md; add `GROQ_API_KEY` and `METRICS_TOKEN` |
| 16 | GAP-NEW-2b | Reconcile memory (`512Mi`) and `--allow-unauthenticated` posture between PRD and DEPLOY.md |

### 🟡 Quality / debt (before v1.4)

| # | ID | Action |
|---|----|--------|
| 17 | BUG-3 | Fix `firebase.json` `public: "web/dist"` → `"web"` |
| 18 | N-M-1 | Remove `**kwargs` passthrough in `safe_fetch`; accept explicit params only |
| 19 | C-P2-2 | Delete dead code: `build_prompt()`, `_IMAGE_REFINE_SYSTEM`, `security/rate_limiter.py` |
| 20 | C-P2-4 | Add `test_firestore_db.py` integration tests against Firestore emulator |
| 21 | U-P1-1 | Fix `text-muted` contrast: use `#94A3B8` on `bg-surface` (#1E293B) |
| 22 | U-P1-3,4,5 | Add `aria-pressed`, arrow-key nav, fix toast live region timing |
| 23 | N-U-P2-3 | Add `aria-live="polite"` to `#error-banner` and `#result` in `web/index.html` |
