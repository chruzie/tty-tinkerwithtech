# Daily Audit — 2026-03-19

**Branch audited:** `ralph/prd-implementation` (most recent code commit: `16af196` — unchanged since 2026-03-17)
**Agents run:** Ralph/PRD · Security · Code Review · UI/UX
**Tests at audit time:** 124 passing, 0 failing
**Previous audits:** [2026-03-16](./2026-03-16-audit.md), [2026-03-18](./2026-03-18-audit.md)
**Net new code commits since 2026-03-18:** 0

---

## Executive Summary

The codebase is identical to the 2026-03-18 audit. All P0/HIGH items remain open — none have been fixed. One positive correction: **OQ-1 (iTerm2 download producing Ghostty format) was incorrectly escalated** in the 2026-03-18 audit; `ghosttyToIterm2()` exists and works in `web/index.html:218`. New findings are surface-level (slug format validation, BYOK key corruption, dangling ARIA references). The pyasn1 CVE remains unpatched.

**Verdict: BLOCK** — same as 2026-03-18. Three P0s, seven HIGHs, one CVE.

---

## Ralph / PRD Gaps

### Correction from prior audit

**OQ-1: RESOLVED (was incorrectly escalated in 2026-03-18 audit)**
`web/index.html` contains `ghosttyToIterm2()` at line 218 — a fully working client-side Ghostty→iTerm2 XML plist converter. Both download buttons produce their correct format. PRD §16 Phase 4 annotation ("OQ-1 escalated") needs updating to reflect this.

### Still open

All items from 2026-03-18 remain open. Top priorities unchanged:

| ID | Severity | Description |
|----|----------|-------------|
| BUG-1 | HIGH | `log_cost()` never called → spend cap non-functional |
| BUG-2 | P1 | Burst offense escalation on allowed request, not rejected one |
| BUG-3 | P1 | `firebase.json` public dir `web/dist` doesn't exist |
| GAP-NEW-2a | MEDIUM | DEPLOY.md provisions dead OAuth secrets |
| GAP-NEW-2b | MEDIUM | DEPLOY.md memory/auth discrepancy vs PRD |
| OQ-2 | Open | Slug collision for unpublished themes |
| OQ-3 | Partially documented | Firestore cursor-based pagination not specced |

### PRD corrections needed

| Section | Fix |
|---------|-----|
| §16 Phase 4 | Remove OQ-1 escalation — iTerm2 download works. Change annotation to reflect BUG-1 (spend cap) as the remaining Phase 4 gap |

---

## Security Findings

**DEP-1:** CVE-2026-30922 in `pyasn1==0.6.2` — **STILL OPEN** (fix: `>=0.6.3`)

### Prior findings — all STILL OPEN, zero FIXED, zero REGRESSED

| ID | Description |
|----|-------------|
| H-1 | Unauthenticated `POST /v1/themes` — gallery flooding |
| H-2/N-H-1 | Stored ANSI injection via `theme.name` in `/v1/neofetch/{slug}` |
| H-4 | Non-constant-time Bearer token comparison on `/metrics` |
| N-H-2 | Negative `offset` in `GET /v1/themes` — Python slice wraps |
| N-H-3 | Token bucket `available()`/`consume()` TOCTOU in async context |
| M-2 | `assert` in `ssrf_guard.py:86` stripped by Python `-O` |
| M-5/C-P0-1 | `increment_download_count` TOCTOU (SQLite) |
| NC-P0-1 | `increment_download_count` TOCTOU (Firestore — should use `Increment`) |
| M-6 | No `max_length` on `PublishRequest.name` / `theme_data` |
| N-M-1 | `safe_fetch` `**kwargs` passthrough to `httpx.get` |
| N-L-3 | S101 ruff suppression too broad (hides `assert` in prod code) |

### New findings

| ID | Severity | Description |
|----|----------|-------------|
| NEW-H-1 | HIGH | `POST /v1/themes/{slug}/download` is unauthenticated with no per-slug rate limit — download counts can be inflated to manipulate `?sort=downloads` gallery ranking |
| NEW-M-1 | MEDIUM | `slug` path parameters have no format/length regex constraint — thousand-char slugs trigger full table scans |
| NEW-M-2 | MEDIUM | (Re-surface of N-M-2) `list_themes(limit=1000)` full-table-scan on every slug lookup; H-1 allows attackers to grow the table |
| NEW-L-1 | LOW | Firestore `upsert_rate_limit` uses `set()` without `merge=True` — future field additions silently deleted |
| NEW-L-2 | LOW | `GenerateRequest.prompt` has no `min_length` — empty/whitespace-only prompts pass model validation |

---

## Code Review Findings

**Test suite: 124 passed, 0 failed.**

### Prior findings — all P0/P1 status unchanged

| ID | Status |
|----|--------|
| C-P0-1 (TOCTOU SQLite) | ❌ STILL OPEN |
| C-P0-2 (`assert` in ssrf_guard) | ❌ STILL OPEN |
| C-P0-3 (TTLCache thread-safety) | ❌ STILL OPEN |
| NC-P0-1 (TOCTOU Firestore) | ❌ STILL OPEN |
| C-P1-1 (no `slug` column / O(N)) | ❌ STILL OPEN |
| C-P1-2 (no ThemeRepositoryProtocol) | ❌ STILL OPEN |

### New issues

| ID | Severity | Description |
|----|----------|-------------|
| NC-P1-NEW | P1 | Firestore `upsert_rate_limit` serializes `burst_timestamps` with `json.dumps` into a string field. Firestore supports native arrays. The dual-path `isinstance(raw_timestamps, str)` handling in `check_rate_limit` is fragile. |
| NC-P2-NEW | P2 | Zero test coverage for the three fix commits (`348bc55`, `ac13878`, `44f66de`). Firestore repo still has zero tests (C-P2-4 from 2026-03-16). |

### Positive notes

- `ac13878` (XFF IP hashing) correctly eliminates shared-proxy-IP rate-limit bypass
- `348bc55` (JSON→key-value prompt) prevents `validate_theme` rejections from LLM confusion
- `44f66de` prevents `AttributeError` crash on Firestore-backed requests
- Test suite stable at 124/124

---

## UI/UX Findings

### Prior findings — all STILL OPEN except partial fixes noted

| ID | Status |
|----|--------|
| U-P0-1 (design system split) | ❌ STILL OPEN |
| U-P0-2 (rate-limit banner) | ✅ FIXED in `web/index.html`; ❌ mockup.html still broken |
| U-P0-3 (no `history.pushState`) | ❌ STILL OPEN |
| U-P1-1 through U-P1-5 (WCAG) | ❌ ALL STILL OPEN |
| N-U-P1-1 through N-U-P1-4 (2026-03-18 P1s) | ❌ ALL STILL OPEN |
| N-U-P2-1 through N-U-P2-4 (2026-03-18 P2s) | ❌ ALL STILL OPEN |

### New issues

| ID | Severity | Description |
|----|----------|-------------|
| NEW-U-P1-1 | P1 | `mockup.html` `switchTab()` (lines 1257–1282) references `#panel-image` and `#tab-image` that don't exist (removed with image mode). Calling `switchTab('image')` hides prompt panel with no replacement. Also: `#panel-prompt` has `aria-labelledby="tab-prompt"` but no `id="tab-prompt"` exists — dangling ARIA reference. |
| NEW-U-P2-1 | P2 | BYOK key overwrite: when a saved key is loaded, field shows `••••••••`. If user types without clearing, the literal bullets + new chars are written to `localStorage`, corrupting the key. No "clear on first keypress" guard. |
| NEW-U-P2-2 | P2 | `web/gallery.html` sort buttons: `setSort()` replaces entire `className` — any `aria-pressed` attributes would be stripped. Sort container has no `role="group"` or `aria-label`. |
| NEW-U-P2-3 | P2 | `web/theme.html` download badge: `parseInt(badge.textContent)` is fragile; clicking both download buttons shows `n+2` locally but fires two separate server increments (correct but diverges if either fails). |
| NEW-U-P3-1 | P3 | Hero animation timer `heroTypingTO` is a single variable; rapid view switching leaks old `setTimeout` callbacks causing double-rendered output lines. |

### Progress assessment

**Static.** Total open issues: 13 (up from 10 at 2026-03-18). New UI surface introduces new debt without addressing existing patterns.

---

## Recommended Actions (Prioritized)

### 🔴 Immediate (7 single-line or small fixes)

| # | ID | Fix |
|---|----|-----|
| 1 | DEP-1 | `uv lock --upgrade-package pyasn1` — patch CVE-2026-30922 |
| 2 | C-P0-1/M-5 | `cache/db.py:233` — `UPDATE themes SET download_count = download_count + 1 WHERE id = ?` |
| 3 | NC-P0-1 | `cache/firestore_db.py:202` — `doc.reference.update({"download_count": firestore.Increment(1)})` |
| 4 | C-P0-2/M-2 | `ssrf_guard.py:86` — `if hostname is None: raise ValueError(...)` |
| 5 | H-4 | `api/main.py:92` — `hmac.compare_digest(auth, f"Bearer {token}")` |
| 6 | N-H-2 | `api/main.py:327` — `offset = max(0, offset)` |
| 7 | N-L-3 | `pyproject.toml` — scope S101 ignore to `tests/*` only |

### 🟠 Before production traffic

| # | ID | Fix |
|---|----|-----|
| 8 | BUG-1 | Wire `log_cost()` after LLM generation in `POST /v1/generate` |
| 9 | H-2/N-H-1 | Strip control chars from `name` in neofetch endpoint (or at publish time) |
| 10 | C-P0-3/N-H-3 | Add `asyncio.Lock` to TTLCache bucket access + atomic `check_and_consume()` |
| 11 | M-6 | `Field(max_length=100)` on `PublishRequest.name`; `max_length=4096` on `theme_data` |
| 12 | NEW-M-1 | Add `Path(pattern=r"^[a-z0-9-]{1,100}$")` to slug path params |
| 13 | C-P1-1 | Add indexed `slug` column to SQLite schema |

### 🟡 Quality / debt

| # | ID | Fix |
|---|----|-----|
| 14 | BUG-3 | `firebase.json` — change `public: "web/dist"` to `"web"` |
| 15 | GAP-NEW-2a | Remove dead OAuth secrets from DEPLOY.md |
| 16 | NEW-U-P1-1 | Remove dead `switchTab('image')` references and dangling `aria-labelledby` |
| 17 | NEW-U-P2-1 | Add "clear on first keypress" to BYOK password field |
| 18 | C-P2-2 | Delete dead code: `build_prompt()`, `_IMAGE_REFINE_SYSTEM`, `security/rate_limiter.py` |

### Trend

Three consecutive daily audits with zero P0 fixes landed. The codebase has been static since 2026-03-17. Recommend a focused fix sprint targeting items 1–7 above (all single-line or small changes) to clear the BLOCK verdict.
